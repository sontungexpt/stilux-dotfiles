[module/packages-count]
type = custom/script
exec = pacman -Q | wc -l
click-left = dunstify "pacman: $(pacman -Qu | wc -l), yay: $([ -x "$(command -v yay)" ] && yay -Qu | wc -l)" -a "polybar-packages-count"
interval = 5
label = "%output%"
format-prefix= "  "
format-background = ${colors.surface0}
format-foreground = ${colors.teal}
format-font = 0
